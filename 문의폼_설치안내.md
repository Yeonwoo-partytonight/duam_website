# 진료 문의 폼 연결 — 설치 안내

문의가 들어오면 **구글 시트에 한 줄씩 쌓이고, 병원 이메일로 알림**이 갑니다.
비용은 없고, 보관 기간 제한도 없습니다. 시트는 언제든 엑셀(.xlsx)로 내려받을 수 있습니다.

준비물: **병원에서 쓰실 구글 계정(지메일) 1개**. 소요 시간 약 15분.

---

## 1단계 — 구글 시트 만들기

1. 병원 구글 계정으로 로그인한 상태에서 <https://sheets.new> 접속
2. 새 스프레드시트가 열리면 왼쪽 위 제목을 **`두암한방병원 문의접수`** 로 변경
3. 시트 내용은 비워둔 채로 두시면 됩니다 (표는 스크립트가 자동으로 만듭니다)

---

## 2단계 — 스크립트 붙여넣기

1. 그 시트 화면에서 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 새 탭이 열리고 `function myFunction() { }` 같은 코드가 보입니다 → **전체 선택 후 삭제**
3. 첨부해 드린 **`문의폼_AppsScript.gs`** 파일을 메모장으로 열어 **내용 전체를 복사** → 붙여넣기
4. 붙여넣은 코드의 **11번째 줄**을 찾아 이메일 주소를 병원 주소로 바꿉니다

```js
var NOTIFY_EMAIL = '여기에_병원_이메일주소@example.com';
```
　↓ 예시
```js
var NOTIFY_EMAIL = 'duam7167160@gmail.com';
```
> 여러 명이 받으시려면 쉼표로 나열하세요 — `'원장님@gmail.com, 데스크@gmail.com'`

5. 상단의 **저장(💾) 아이콘** 클릭

---

## 3단계 — 배포하기 (가장 중요)

1. 오른쪽 위 파란색 **배포 → 새 배포** 클릭
2. 왼쪽 **⚙️(톱니바퀴) → 웹 앱** 선택
3. 아래처럼 설정합니다

| 항목 | 선택할 값 |
|---|---|
| 설명 | `문의폼` (아무렇게나) |
| 다음 사용자로 실행 | **나 (본인 계정)** |
| 액세스 권한이 있는 사용자 | **모든 사용자** ← 꼭 이걸로! |

> ⚠️ "액세스 권한"을 '나만'으로 두면 홈페이지에서 접수가 안 됩니다.

4. **배포** 클릭 → 권한 승인 창이 뜹니다
   - **액세스 승인** → 병원 구글 계정 선택
   - "Google에서 확인하지 않은 앱입니다" 경고가 나오면
     → **고급** → **`두암한방병원 문의접수`(안전하지 않음)으로 이동** 클릭
     → (본인이 직접 만든 스크립트라 정상입니다)
   - **허용** 클릭
5. 마지막에 나오는 **웹 앱 URL**을 복사합니다. 이렇게 생겼습니다:

```
https://script.google.com/macros/s/AKfycbx....................../exec
```

**이 주소를 저에게 알려주시면 홈페이지에 넣어드립니다.**
직접 하시려면 `index.html`에서 `여기에_배포ID`를 찾아 바꾸시면 됩니다:

```html
<form id="inquiryForm" ... action="https://script.google.com/macros/s/여기에_배포ID/exec" method="POST">
```

---

## 4단계 — 확인

1. 복사한 웹 앱 URL을 브라우저 주소창에 그대로 붙여넣고 엔터
   → `{"result":"ok","message":"두암한방병원 문의 접수 스크립트가 작동 중입니다."}` 가 보이면 정상입니다
2. 홈페이지에 주소를 넣은 뒤, 실제로 본인 이름·번호로 한 번 접수해 보세요
3. 구글 시트에 줄이 추가되고, 지정한 메일로 알림이 오면 완료입니다

---

## 시트에 쌓이는 모양

| 접수 일시 | 성함 | 연락처 | 전화 받기 편한 시간 | 개인정보 동의 | 처리 상태 |
|---|---|---|---|---|---|
| 2026-08-03 14:22 | 홍길동 | 010-1234-5678 | 오전 (09:00-12:00) | 동의 | 접수 |

- **처리 상태** 칸은 병원에서 직접 `완료`, `부재중` 등으로 고쳐 쓰시면 됩니다
- 엑셀로 받으려면 시트에서 **파일 → 다운로드 → Microsoft Excel(.xlsx)**

---

## 참고 — 코드를 수정했을 때

스크립트 내용을 고친 뒤에는 **배포 → 배포 관리 → 연필(✏️) → 버전: 새 버전 → 배포**를 해야 반영됩니다.
이때 **URL은 그대로 유지**되므로 홈페이지는 다시 안 고쳐도 됩니다.
(실수로 '새 배포'를 누르면 URL이 바뀌니 주의하세요)

---

## 아직 남은 숙제

이번 작업으로 **동의 절차와 고지**는 폼 안에 들어갔지만, 개인정보보호법 제30조는
**개인정보처리방침을 홈페이지에 상시 공개**하도록 따로 요구합니다.
푸터에 「개인정보처리방침」 링크와 페이지를 하나 만들어 두는 작업이 남아 있습니다.
말씀해 주시면 병원 상황에 맞춰 초안을 만들어 드리겠습니다.
