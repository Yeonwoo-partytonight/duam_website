# 두암한방병원 홈페이지 보완 점검

**대상**: `index.html` (2,888줄 / 189KB)
**점검일**: 2026-08-03
**관점**: 법적 리스크 · 접근성(고령층) · SEO/전환 · 코드 품질

> **진행 상황 (2026-08-03 갱신)**
> `1-1 문의 폼`과 `1-3 개인정보 동의`는 **처리 완료**(커밋 `2681594`). 나머지는 미착수 상태입니다.
> 줄 번호는 최초 점검 시점(수정 전) 기준이라, 문의 폼 수정 이후 2300줄 이후 구간은 실제 위치가 30줄 정도 밀려 있습니다.

---

## 🔴 1순위 — 지금 실제로 손해가 나고 있는 것

### ✅ 1-1. 문의 폼이 100% 실패 중 (2308줄) — **처리 완료**

```html
<form id="inquiryForm" action="https://formspree.io/f/여기에_폼ID" method="POST">
```

`여기에_폼ID`가 미교체 플레이스홀더였습니다. 사용자가 이름·연락처·증상을 다 적고 "문의 남기기"를 누르면 404 → 항상 "전송에 실패했습니다" 메시지만 떴습니다. **온라인 문의가 전량 유실되던 상태.**

**처리 내역** — Formspree를 폐기하고 **Google Apps Script + 구글 시트** 방식으로 전환했습니다.
- Formspree 무료 플랜은 월 50건 + **제출 이력 30일만 보관** + **CSV 내보내기가 유료 전용**이라, 문의 기록을 엑셀로 쌓아두려는 목적에 맞지 않았습니다.
- Apps Script는 무료·영구 보관이고 구글 시트 자체가 스프레드시트라 `.xlsx` 다운로드가 바로 됩니다. 접수 시 병원 메일로 알림도 함께 발송됩니다.
- 전송은 `URLSearchParams` + `application/x-www-form-urlencoded`로 보냅니다 (CORS preflight 회피용 — `FormData`를 그대로 보내면 동작하지 않습니다).

**남은 한 단계** — Apps Script 배포 후 받은 웹 앱 주소로 `action`의 `여기에_배포ID`를 교체해야 실제로 접수가 시작됩니다. 절차는 저장소의 `문의폼_설치안내.md` 참고.

### 1-2. 환자 후기 게시 — 의료법 제56조 위반 소지

두 곳에 치료경험담이 있습니다. (1367~1369줄 `#reviews`, 2328~2330줄 `#news-review`)

> "허리 통증으로 여러 병원을 다녀봤는데 **이곳이 제일 효과가 좋았어요**"

의료법 56조 2항 2호(치료경험담 광고 금지) + 3호(타 의료기관 비교 광고) 양쪽에 걸립니다. 자사 홈페이지 게시도 금지 대상으로 해석되는 것이 일반적입니다.

→ 후기 카드는 삭제하고, 이미 있는 "네이버 지도에서 확인" 외부 링크(2332~2335줄)만 남기는 방식이 안전합니다.

### 🔶 1-3. 개인정보 수집 동의 절차 없음 (2308~2316줄) — **대부분 처리, 일부 잔여**

당시 고지는 안내문 두 줄뿐이었고 다음이 전부 없었습니다.

- 동의 체크박스 (`required`) + 수집항목/목적/보유기간/거부권 고지 → 개인정보보호법 15·22조
- **문의 내용에 "증상"을 받으므로 민감정보(건강정보)** → 23조상 **분리된 별도 동의** 필요
- Formspree는 해외 사업자 → 국외 이전 동의·고지(28조의8)
- **사이트 전체에 개인정보처리방침 링크 0건** (30조 공개 의무). 푸터에도 없음.

**처리 내역**
- **증상 입력란(textarea)을 아예 삭제**했습니다. 성함 · 연락처 · 전화 받기 편한 시간만 받는 '연락 요청' 폼으로 축소 → 민감정보를 수집하지 않으므로 23조 분리 동의 요건이 소멸합니다. 대신 "증상 등 건강에 관한 내용은 적지 마시고 전화 상담 시 말씀해 주세요" 안내를 넣었습니다.
- 필수 동의 체크박스 + 고지 5항목(수집항목 / 수집목적 / 보유기간 / 국외이전(Google LLC, 미국) / 거부 권리)을 폼 안에 추가했습니다. 미체크 시 제출이 차단되는 것을 브라우저에서 확인했습니다.

**❗ 잔여** — 개인정보보호법 **30조는 개인정보처리방침의 상시 공개를 별도로 요구**합니다. 폼 안의 동의 고지로 갈음되지 않습니다. 푸터에 「개인정보처리방침」 링크와 페이지를 만드는 작업이 남아 있습니다.

### 1-4. JS가 실패하면 화면이 완전 백지

`.route { display:none }`(1058줄) 구조라 라우터가 클래스를 붙이기 전까지 본문 전체가 숨겨져 있습니다. GNB·드로어·푸터 링크·subnav도 전부 JS 생성이고 `route()` 호출은 2859줄(스크립트 거의 끝)입니다.

- `<noscript>` 폴백 0건
- 2559~2765줄 사이 예외가 하나만 나도 백지

→ `route()`를 렌더 함수보다 먼저 호출 + `try/catch` 격리, `<noscript><style>.route{display:block}</style></noscript>` 추가.

### 1-5. 측정 코드 전무

`gtag` / GA4 / 네이버 애널리틱스 / 서치어드바이저·서치콘솔 소유확인 메타 — **전부 0건**.

전화 링크 19곳, 톡톡 3곳이 있는데 어느 진료과가 전화를 만드는지 알 수 없는 상태입니다.
※ 해시 라우팅이므로 `hashchange`(2858줄)에서 수동 page_view를 보내야 합니다. 안 그러면 전 세션이 페이지뷰 1회로 집계됩니다.

---

## 🟠 2순위 — 고령층 접근성 (이 사이트의 핵심 원칙)

### 2-1. 색 대비가 WCAG AA에 크게 미달 (실측 계산)

| 토큰 | 값 | 흰 배경 대비 | 기준 |
|---|---|---|---|
| `--text-light` | `#92ABBE` | **2.39 : 1** | 4.5 |
| `--text-mid` | `#5D7B93` | **4.45 : 1** | 4.5 |
| `.btn-primary` 흰글씨 / `--sky` | `#5BA4CF` | **2.74 : 1** | 4.5 |
| `.nav-cta` 흰글씨 / `--coral` | `#F07B6E` | **2.71 : 1** | 4.5 |
| `.btn-outline` `--sky2` | `#3D89BD` | **3.81 : 1** | 4.5 |
| 별점 ★ | `#F5C842` | **1.59 : 1** | — |

문제는 `--text-light`가 **전부 13~15px 작은 글씨**에 쓰인다는 점입니다 — 진료시간 "점심 12:00–14:00"(1383·1593줄), 공지 날짜, 후기 작성자, 폼 안내문 등 11곳.

→ `--text-light` → `#6B8299` 이상, `--text-mid` → `#4F6A80` 이상, `--sky2` → `#2F6E99` 정도로 조정하면 전부 통과합니다. (토큰만 바꾸면 전역 반영)

### 2-2. `html { font-size: 18px }` px 고정 (79줄)

루트를 px로 고정하면 브라우저 "기본 글꼴 크기"를 키워 쓰는 사용자(= 고령층이 가장 많이 쓰는 설정)의 값이 **무시**됩니다. `font-size: 112.5%`로 바꾸면 렌더링은 동일하면서 사용자 설정을 존중합니다.

### 2-3. 닫힌 드로어에 키보드 포커스가 들어감 (309~317, 1160줄)

`.drawer-panel`이 `transform: translateX(100%)`로 화면 밖에만 있고 `visibility`/`display` 변화가 없어, Tab을 누르면 **포커스가 화면 밖 메뉴로 사라집니다**. 게다가 `aria-hidden="true"` 컨테이너 안에 포커스 가능 요소가 있는 구조 자체가 접근성 위반입니다.

→ 닫힘 시 `visibility: hidden` 또는 `inert`.

### 2-4. 드로어에 ESC 닫기·포커스 트랩·포커스 복귀 전부 없음

파일 전체에 `.focus()` 호출이 **0건**입니다. ESC 처리(2749줄)는 메가메뉴 전용이라 드로어는 ESC로 못 닫습니다.

### 2-5. 페이지 전환 시 스크린리더가 아무것도 모름 (2837~2850줄)

`showRoute()`가 클래스 토글과 스크롤만 합니다.

- 포커스 이동 없음 → 드로어 링크로 이동하면 **포커스가 `aria-hidden` 요소 안에 갇힙니다**
- `document.title` 갱신 없음 → 9개 페이지가 전부 같은 제목
- live region 없음 → 화면이 바뀐 사실이 전달 안 됨

### 2-6. "본문 바로가기" 링크 없음

키보드 사용자는 매번 로고 → GNB 4개 → 전화 CTA → 햄버거 → (닫힌 드로어 링크 20여 개) → subnav를 다 지나야 본문에 닿습니다.

### 2-7. 그 외

- 흐르는 공지 배너: 정지 수단이 마우스 hover뿐 (537줄) — 키보드·터치 사용자는 못 멈춤 (WCAG 2.2.2). 게다가 대비 2.62:1로 읽기도 어려움
- `prefers-reduced-motion`: 아코디언·마퀴만 대응, 스크롤 리빌·히어로 애니메이션·`scroll-behavior: smooth`는 그대로
- 터치 타깃 44px 미만: 햄버거 40px(279줄), 드로어 하위메뉴 36px(376줄), 푸터 링크 ~18px(704줄), 소형 폰 전화 CTA 37px(997줄)
- 폼: 필수 표시 없음, 필드별 에러 안내 없음, `outline: none`(940줄), 성공 시 폼을 통째로 교체해 포커스 소실(2819줄)
- `aria-current` 0건 / 탭 UI에 `role="tab"`·`aria-selected` 없음 / GNB 4개 링크에 `aria-expanded="true"` 동시 부여(2691줄)
- `.notice-row`에 `cursor:pointer`인데 링크도 핸들러도 없음 (903·2181줄)

---

## 🟡 3순위 — SEO · 성능 · 전환

### 3-1. 해시 라우팅의 구조적 한계

콘텐츠는 초기 HTML에 다 들어있어 **색인은 됩니다.** 다만 URL이 `https://duamhanbang.co.kr/` 하나뿐이고, canonical도 루트 고정(8줄)입니다. `#/spine`은 sitemap에 넣어도 무의미합니다(검색엔진은 `#` 이후를 URL로 취급 안 함).

또 `<h1>`이 한 문서에 9개, `<h3>`/`<h4>`는 0개 — 카드 제목들이 전부 `div`입니다.

→ **근본 해결은 진료 페이지를 실제 경로(`/spine/`)의 개별 파일로 분리**하는 것뿐. 단일 파일 유지 시 차선책은 라우터에서 title/description/canonical/OG를 route별로 갱신하는 것입니다. (이건 결정이 필요한 사안)

### 3-2. 이미지 — webp 변환이 원장 사진 3장에만 적용됨

| 구분 | 현황 |
|---|---|
| 원장 사진 3장 | png 588~666KB → webp 40~50KB (**92% 절감**) ✅ |
| facility-*.jpg 13장 | 120~240KB **원본 그대로** ❌ |
| equipment-*.jpg 3장, treatment-chuna.jpg | 119~201KB **원본 그대로** ❌ |

`#/about` 한 페이지에서만 **약 1.5~2MB**가 내려갑니다. 원장 사진과 같은 방식으로 처리하면 가장 비용 대비 효과가 큰 개선입니다.

또 schema.org 37줄이 **666KB png를 직접 참조**합니다.

### 3-3. 폰트 웨이트 3종이 안 쓰이는데 로드됨 (72줄)

- Noto Sans KR 300 → `font-weight:300` 선언 **0건**
- Noto Serif KR 300·400 → Serif 사용처 11곳 전부 `600`
- DM Serif Display italic → `font-style:italic` 선언 **0건**

한글 웹폰트는 웨이트 1개당 `@font-face` 규칙이 100개 안팎 생성됩니다. **7 → 3개로 줄이면 렌더 블로킹 CSS가 절반 이하**로 줍니다. (5분 작업)

### 3-4. OG / 공유

- 1200×630 전용 이미지 없음 — 세로형 시설 사진을 그대로 사용. `twitter:card="summary_large_image"`와 안 맞음
- `og:image:width/height`가 주석 처리(16~18줄) → **카카오톡 첫 공유 시 미리보기가 안 뜨면 그대로 캐시**됩니다
- route별 OG 갱신 없음 → `#/accident`를 카톡에 공유해도 홈 정보로 뜸
- 이미지 경로가 OG는 퍼센트 인코딩, 본문 `<img>`는 원문 한글로 이원화 → 서버에 따라 한쪽만 404 가능. `images/` 같은 ASCII 폴더명 권장

### 3-5. 구조화 데이터 보강 여지

- `sameAs` 없음 — **푸터에 블로그·인스타·톡톡 링크가 이미 있습니다**(2370~2372줄). 가장 손쉬운 개선
- `postalCode`, `geo`, `priceRange`, `hasMap` 없음 → 지역 검색(로컬 팩)에서 손해
- 39줄 `"PhysicalTherapy"`는 `MedicalSpecialty` 열거값이 아님(무효 처리 가능). 한방병원인데 물리치료 2개만 등록된 것도 실제 진료 범위와 괴리
- **FAQPage 적용 가능** — 1972~2003줄 교통사고 FAQ 3건이 이미 `<details>` 구조로 완비. 리치 결과 후보로 즉시 적용 가능
- ⚠️ `aggregateRating`은 넣지 마세요 — "4.5 네이버 평점"은 제3자 데이터라 자체 마크업 시 정책 위반 소지

### 3-6. 전환 요소

- 카카오톡 버튼이 주석 처리(2383~2389줄) — CSS는 이미 준비됨. **채널 ID만 받으면 즉시 활성화**
- 지도가 구글맵(1421·1607줄) — 어르신 환자층엔 네이버·카카오맵 길찾기 연결이 실효가 큽니다. 현재 "길찾기" 진입점이 없음
- 네이버 예약(booking.naver.com) 연결 없음 — 톡톡만 있음
- 모바일 하단 전체폭 고정 CTA 없음 (우하단 48px 원형 2개뿐)

**잘 되어 있는 것**: `tel:` 링크 19곳 번호 전부 일치, 외부 링크 전부 `rel="noopener"`+"(새 창)" 명시, `alt` 20개 전량 작성, 중복 `id` 0건, `href="#"` 0건, gzip 43.7KB (문제 없음)

---

## 🟢 4순위 — 콘텐츠 정확성 · 코드 정리

### 4-1. 정보 불일치 (사실관계 오류)

| 위치 | 문제 |
|---|---|
| 1228줄 공지 배너 | "**진료** 마감 17:00, 토요일 마감 12:00" → 실제로는 **접수** 마감. 진료는 17:30/12:30(1383줄, schema 58·64줄, 푸터). 30분 차이 |
| 1165·1180·1196줄 | "개원 **9년차**" vs 1247줄 "SINCE **2016**" → 2026년 기준 10~11년차. 4곳 하드코딩 |
| 2181줄 공지 4건 | 전부 **2025년** 날짜. 첫 항목엔 `new` 강조 배지 |
| 2377줄 | `© 2025` 고정 |
| 1424·1609줄 | 주소는 "산수동"인데 안내는 "두암3동 행정복지센터 인근" — 사실 확인 필요 |
| 1197·1254·1356줄 | 네이버 평점 `4.5`가 3곳 하드코딩 — 실제 평점과 달라지면 허위·과장 광고 |

### 4-2. 미완성 콘텐츠

- 원장 2명 약력이 `<!-- TODO -->` + "약력 정보는 준비 중입니다" (1489·1505줄)
- 건강칼럼 3편에 `<!-- 병원 감수 대기 -->` 표시된 채 게시 중 (2193·2211·2229줄)
- 비급여 수가 5개 항목 전부 "상담 후 안내"/"항목별 상이" (2285~2289줄) → 의료법 45조 비급여 고지 의무. 최소한 가격 범위 표기 필요
- 동영상·보도자료 섹션 통째로 주석 처리 (2250~2275줄)
- `<span class="vp-num"></span>` 빈 요소 2곳 (1942·1968줄)

### 4-3. 의료광고 표현 손질

- **"전문"** 표기 (7·14·23·1212·1264줄) — 전문병원 지정 없이 "○○ 치료 전문"은 오인 소지. "진료"로 완화 권장
- **"치료비 걱정 없이" / "본인 부담 없이"** (1406·1967줄) — 의료법 27조 3항 환자 유인 해석 논란 문구
- 단정적 효과 표현 — "더 빠르고 효과적인 치료를 **실현**"(1263), "**빠르게** 회복"(1264), "치료 효과 **강화**"(1339), "면역·항염 **효과**"(1341), "**빠른** 활력 회복과 면역 보강"(2115)
- ("완치·100%·보장" 표현은 없음 ✅)

### 4-4. 실제 버그

| 줄 | 내용 |
|---|---|
| 864줄 `.pc-ph` | `position:absolute`인데 `<img>`는 정적 배치 → **페인팅 순서상 플레이스홀더 아이콘이 사진 위에 55% 불투명도로 항상 덮입니다** (시설 사진 14곳). 의도는 "로드 실패 시 대체"였던 것으로 보임 |
| 2857줄 | 드로어를 닫는 유일한 경로가 `hashchange` → **같은 링크를 다시 누르면 해시가 안 바뀌어 드로어가 열린 채 화면을 덮습니다** |
| 79 + 2849줄 | `scroll-behavior: smooth` + 2인자 `scrollTo(0,0)` → 페이지 전환 시 새 화면이 뜬 상태에서 천천히 위로 스크롤됨. `{top:0, behavior:'auto'}`로 통일 |
| 1453~1498줄 | `<picture>`의 `<source>` 폴백은 **포맷 미지원 시에만** 동작, **404에는 동작 안 함**. webp가 없으면 png가 있어도 깨짐. 시설 사진엔 다 있는 `onerror`가 원장 사진 3장에만 없음 |
| 944·950줄 | `:disabled`와 `:hover` 명시도가 같고 `:hover`가 뒤에 있어 **전송 중 비활성 버튼에도 hover 효과 적용** |
| 2837줄 | `#/foo` 같은 잘못된 해시 입력 시 홈이 뜨지만 주소창은 `#/foo`로 남음 |

### 4-5. 반응형

- **421~480px 구간 헤더 넘침 가능성** — 압축 규칙이 420px 이하에서만 적용(994~1004줄). 루트 18px 기준 개략 계산 시 헤더에 약 470px 필요 → **428px(iPhone 14 Plus) / 430px(15 Pro Max)에서 겹치거나 잘림**. `body{overflow-x:hidden}`(84줄) 때문에 가로 스크롤로 복구도 안 됨. 분기점을 480px로 올릴 것 권장 (실기기 확인 필요)
- 표에 가로 스크롤 래퍼 없음 — `.hours-table`(4열, 1380·1590줄), `.price-table`(2282줄). 좁은 화면에서 "상태" 열이 잘려 안 보일 수 있음
- 627줄 `.treat-scroll-wrap { margin: 0 -28px }` — 560px 이하에서 섹션 패딩은 20px로 줄지만 음수 마진은 그대로라 좌우 8px씩 넘침

### 4-6. 정리하면 좋은 것

- 죽은 CSS: `.info-grid.cols-4`(797·975·1023), `.drawer-div`(401), `.drawer-section-label`(338), `.ic-tag`(869), `.doc-career` 계열(846~855), `.float-btn.kakao`(736)
- 하드코딩 색상: `#D5ECFB`(481·751 중복), `#F5C842`(647·653 중복), `#FFF0EE`(669 — `--coral-pale`와 값이 완전히 동일), `#FFF7E0`/`#C89520`(670), `#4FA8D8`(320), `#c7e6f7`/`#ffd5d0`/`#b5e5de`(634~636)
- `@media print` 0건 — 진료시간표·오시는 길 인쇄 수요가 높은 사이트인데 고정 헤더·플로팅 버튼이 본문 위에 겹쳐 출력됨
- `prefers-color-scheme` / `<meta name="color-scheme">` 0건 — 다크모드 브라우저에서 폼 입력창 색이 뒤집힐 수 있음
- 스크롤 리스너 3개(2565·2756·2861줄) 모두 non-passive + `offsetTop` 강제 리플로우
- `.rv`("scroll reveal")에 실제 스크롤 감지가 없음 — 라우트 진입 시 한 번에 전부 `.in` 부여. IntersectionObserver 코드 없음

---

## 권장 처리 순서

| # | 항목 | 예상 | 상태 |
|---|---|---|---|
| 1 | ~~Formspree 폼 ID 교체~~ → Apps Script + 구글 시트 연결 | 5분 | ✅ 완료 (배포 URL 교체만 남음) |
| 2 | 후기 섹션 2곳 삭제 + "전문"·유인 문구 정리 | 30분 | |
| 3 | ~~개인정보 동의 체크박스~~ + 개인정보처리방침 페이지 | 2시간 | 🔶 동의 완료 / 처리방침 미착수 |
| 4 | 색상 토큰 3개 조정 (`--text-light`/`--text-mid`/`--sky2`) + `font-size: 112.5%` | 20분 | |
| 5 | 공지 배너 "접수 마감" 수정 · "9년차" · 공지 날짜 · `© 2025` | 20분 | |
| 6 | `<noscript>` 폴백 + `route()` try/catch | 30분 | |
| 7 | 폰트 웨이트 7→3 | 5분 | |
| 8 | `.pc-ph` 겹침, 드로어 닫힘 버그, `scrollTo` smooth | 30분 | |
| 9 | GA4 + 서치콘솔/서치어드바이저 (hashchange page_view 포함) | 1시간 | |
| 10 | 드로어 `inert`/ESC/포커스, skip-link, 라우트 전환 시 title·focus | 2시간 | |
| 11 | facility·equipment jpg 17장 webp 변환 + `<picture>` | 2시간 | |
| 12 | schema `sameAs`·`geo`·`postalCode` 보강 + FAQPage | 1시간 | |
| 13 | 421~480px 헤더 분기, 표 가로 스크롤 래퍼 | 1시간 | |
| 14 | 카카오 채널 활성화, 네이버 길찾기 링크 | 15분 | |
| — | **해시 라우팅 → 실제 경로 분리** (구조 결정 필요) | 大 | |

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-03 | 최초 전면 점검 |
| 2026-08-03 | 문의 폼 Apps Script 연결 + 개인정보 동의 절차 추가 (커밋 `2681594`) |
