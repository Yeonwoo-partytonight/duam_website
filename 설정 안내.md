# 관리자페이지 설정 안내

공지사항·건강 칼럼을 홈페이지에서 직접 올리고 고칠 수 있게 만드는 1회성 설정입니다.
아래 1~5단계를 순서대로 한 번만 해두면, 이후로는 홈페이지에서 글만 쓰시면 됩니다.

전체 소요 시간은 20~30분 정도입니다. 중간에 막히면 그 단계 번호를 알려주세요.

---

## 전체 그림

```
관리자페이지(#/admin)에서 저장
        ↓
   저장 서버(Cloudflare Worker)   ← 비밀번호를 여기서 확인
        ↓
   GitHub 저장소에 자동 커밋
        ↓
   홈페이지에 반영 (1~2분 소요)
```

GitHub에 글을 쓰려면 "토큰"이라는 열쇠가 필요한데, 이 열쇠를 홈페이지에 그냥 두면
누구나 열어볼 수 있습니다. 그래서 열쇠는 저장 서버에만 보관하고, 홈페이지에서는
비밀번호만 보내도록 만들었습니다.

---

## 준비물

- GitHub 계정 (이미 있으심 — 저장소: `Yeonwoo-partytonight/duam_website`)
- Cloudflare 계정 (2단계에서 무료로 만듭니다)
- 컴퓨터에 Node.js 설치 — https://nodejs.org 에서 왼쪽 LTS 버튼으로 설치
  - 이미 설치돼 있는지 확인: 터미널에 `node -v` 입력 → `v20.x.x` 같은 게 나오면 OK

---

## 1단계. GitHub 토큰(열쇠) 만들기

1. https://github.com/settings/personal-access-tokens 접속
2. 오른쪽 위 **Generate new token** 클릭
3. 아래대로 채웁니다.

   | 항목 | 입력할 값 |
   |---|---|
   | Token name | `duam-admin` |
   | Expiration | `90 days` (고를 수 있는 최대값입니다. 90일마다 다시 만들어야 합니다 — 아래 "알아두실 점" 참고) |
   | Repository access | **Only select repositories** 선택 → `duam_website` 고르기 |

4. **Permissions → Repository permissions** 를 펼치고, **Contents** 항목만
   `Read and write` 로 바꿉니다. (나머지는 손대지 않습니다)
5. 맨 아래 **Generate token** 클릭
6. `github_pat_` 로 시작하는 긴 글자가 나옵니다. **이 화면을 벗어나면 다시 볼 수 없으니**
   메모장에 잠깐 붙여넣어 두세요. (4단계에서 씁니다)

> 이 토큰은 이 저장소의 파일을 고치는 것 외에는 아무것도 못 합니다.

---

## 2단계. Cloudflare 계정 만들기

1. https://dash.cloudflare.com/sign-up 에서 이메일·비밀번호로 가입
2. 메일로 온 인증 링크 클릭
3. 요금제를 고르라고 나오면 **무료(Free)** 로 두시면 됩니다.
   (이 용도로는 무료 범위를 넘길 일이 없습니다)

---

## 3단계. 저장 서버 올리기

터미널(맥은 "터미널", 윈도우는 "PowerShell")을 열고 아래를 한 줄씩 실행합니다.

```bash
cd "/Users/qodusdn/Desktop/etc/MY/두암한방병원/worker"
npx wrangler login
```

- 처음이면 설치할지 물어봅니다 → `y` 입력
- 브라우저가 열리면서 Cloudflare 로그인·허용 화면이 뜹니다 → **Allow** 클릭

이어서 배포합니다.

```bash
npx wrangler deploy
```

성공하면 마지막 줄쯤에 주소가 나옵니다.

```
Published duam-admin
  https://duam-admin.○○○○.workers.dev
```

**이 주소를 메모해 두세요.** (5단계에서 씁니다)

---

## 4단계. 비밀번호와 열쇠 등록하기

같은 폴더에서 이어서 실행합니다.

```bash
npx wrangler secret put ADMIN_PIN
```

입력하라고 나오면 **관리자 비밀번호로 쓰실 숫자**를 넣고 엔터.

> **6자리를 권합니다.** 4자리는 경우의 수가 1만 개뿐이라 자동으로 대입해보는 공격에
> 상대적으로 약합니다. 생일·전화번호처럼 쉽게 떠올릴 수 있는 숫자는 피해주세요.

다음으로 1단계에서 받은 GitHub 토큰을 등록합니다.

```bash
npx wrangler secret put GITHUB_TOKEN
```

입력하라고 나오면 `github_pat_...` 전체를 붙여넣고 엔터.
(붙여넣어도 화면에 글자가 안 보이는 게 정상입니다)

여기까지 하면 메모장에 적어둔 토큰은 지우셔도 됩니다.

---

## 5단계. 홈페이지에 저장 서버 주소 알려주기

1. `index.html` 파일을 열고 `REPLACE-ME` 를 찾습니다. (찾기: Ctrl+F 또는 ⌘+F)
2. 이런 줄이 있습니다.

   ```js
   var ADMIN_API = 'https://REPLACE-ME.workers.dev';
   ```

3. 3단계에서 메모한 주소로 바꿉니다.

   ```js
   var ADMIN_API = 'https://duam-admin.○○○○.workers.dev';
   ```

4. 저장한 뒤 GitHub에 올립니다.

   ```bash
   cd "/Users/qodusdn/Desktop/etc/MY/두암한방병원"
   git add index.html
   git commit -m "[정보수정] 관리자페이지 저장 서버 주소 연결"
   git push
   ```

1~2분 뒤 **https://duamhanbang.co.kr/#/admin** 에 들어가서 비밀번호를 넣어보세요.
관리 화면이 나오면 설정 완료입니다.

---

## 6단계 (선택). 비밀번호 자동 대입 추가 차단

저장 서버에도 "여러 번 틀리면 늦추고 10분간 막는" 장치가 들어 있지만,
서버가 여러 대로 나뉘어 돌 때는 완벽하지 않습니다. 더 확실히 막으려면:

1. Cloudflare 대시보드 → 왼쪽 **Workers & Pages** → `duam-admin` 클릭
2. **Settings → Rate limiting** 에서 규칙 추가
3. 예: 같은 IP에서 **1분에 20회** 넘게 요청하면 차단

안 하셔도 동작에는 문제 없습니다.

---

## 사용법

주소창에 **duamhanbang.co.kr/#/admin** 을 직접 입력해 들어갑니다.
(홈페이지 메뉴에는 일부러 넣지 않았습니다. 일반 방문자에게는 보이지 않습니다)

들어갈 때 **비밀번호**와 함께 **이름**을 고르시게 되어 있습니다.
김미성 원무과장님 / 배광희 원장님 / 관리자 / 기타 중에서 고르시면 되고,
**기타**를 고르시면 아래에 입력칸이 하나 나옵니다. 이름을 적으셔도 되고, 비워 두셔도 됩니다.
(비워 두시면 "이름 없음"으로 기록됩니다)

이름은 비밀번호가 아니라, 나중에 "누가 이 글을 올렸더라" 하실 때 보시려고 남기는 표시입니다.
고른 이름은 **최근 활동** 탭에 기록됩니다.

### 공지사항

- **＋ 새 공지 쓰기** — 제목만 써도 되고, 자세한 내용이 필요하면 아래 "내용"에 적습니다.
- **올라갈 날짜** — 오늘보다 뒤 날짜를 넣으면 그날이 되어야 홈페이지에 보입니다(예약).
- **상태** — `게시`만 홈페이지에 나옵니다. 잠깐 내리고 싶으면 `숨김`으로 바꾸세요.
  (삭제하지 않아도 되고, 나중에 다시 `게시`로 돌리면 됩니다)
- **상단 고정** — 중요한 공지를 목록 맨 위에 붙박이로 둡니다. 최대 3개까지.
- 여기 올리신 공지는 **홈 화면 맨 위에 흐르는 띠**에도 자동으로 나옵니다.
  고정해 두신 공지가 먼저, 그다음 최근 공지 순으로 최대 6개까지 보입니다.
  올린 공지가 하나도 없으면 원래 있던 안내 문구(전화번호·주소 등)가 그대로 나옵니다.

### 건강 칼럼

- **진료 분류** — 고른 분류에 따라 글 아래에 해당 진료 페이지로 가는 버튼이 자동으로 붙습니다.
- **유튜브 영상 링크** — 유튜브에서 **공유** 버튼을 눌러 나온 주소를 그대로 붙여넣으시면,
  글 제목 아래에 영상이 함께 나옵니다. 넣지 않으셔도 됩니다.
  주소 형태는 아무거나 괜찮습니다(`youtu.be/...`, `youtube.com/watch?v=...` 등).
  유튜브 주소가 아니면 저장할 때 알려 드립니다.
- **대표 사진** — 파일을 고르면 알아서 크기를 줄여 올립니다. 따로 준비할 것 없습니다.
- **미리보기** — 저장 전에 홈페이지에서 어떻게 보이는지 그대로 확인할 수 있습니다.
- **임시저장** — 아직 다 못 쓴 글은 임시저장으로 두면 홈페이지에 나오지 않습니다.
- **미리보기 링크 복사** — 임시저장한 글 옆에 이 단추가 생깁니다.
  누르면 주소가 복사되고, 그 주소를 받으신 분은 글이 아직 올라가지 않았어도 미리 보실 수 있습니다.
  감수를 부탁드릴 때 카카오톡·문자로 보내시면 됩니다.
  (메뉴 어디에도 없는 주소이고, 검색에도 걸리지 않게 해두었습니다)

### 휴지통

글을 **삭제**해도 바로 없어지지 않고 **휴지통**으로 옮겨집니다.

- **되살리기** — 지우기 전 상태(게시/숨김/임시저장)로 그대로 돌아갑니다.
- **완전 삭제** — 정말로 지웁니다. 이건 되돌릴 수 없습니다.
- 휴지통에 있는 글은 **7일**이 지나면 목록에서 사라져 되살릴 수 없습니다.
  실수로 지우셨다면 그 안에 되살려 주세요.

### 최근 활동

누가·언제·무엇을 했는지 최근 100건까지 표로 보여 드립니다.
"이 공지 누가 지웠지?" 싶을 때 여기를 보시면 됩니다.
저장 직후에는 바로 안 보일 수 있고, 1~2분 뒤 탭을 다시 누르시면 나옵니다.

### 글 쓸 때 쓰는 간단 서식

| 이렇게 쓰면 | 이렇게 보입니다 |
|---|---|
| 빈 줄 한 줄 넣기 | 문단이 나뉩니다 |
| `**중요한 말**` | **중요한 말** (굵게) |
| 줄 앞에 `- ` | 체크 목록 항목 |
| `[전화 문의](tel:062-716-7160)` | 누르면 전화가 걸리는 링크 |

"굵게 / 목록 / 링크 / 사진 넣기" 버튼을 누르면 알아서 들어가니 외우지 않으셔도 됩니다.

---

## 알아두실 점

**저장 후 1~2분 걸립니다.**
관리 화면에서는 바로 바뀌지만, 실제 홈페이지에 퍼지는 데 시간이 조금 걸립니다.
바로 안 바뀐다고 여러 번 저장하지 마시고 잠시 기다렸다 새로고침해 주세요.

**"숨김"·"임시저장"·"휴지통" 글도 완전히 감춰지는 것은 아닙니다.**
홈페이지 화면에는 안 나오지만, 글 내용 자체는 공개 저장소 파일에 남습니다.
휴지통에서 **완전 삭제**를 하셔야 파일에서도 지워집니다.
환자 개인정보나 대외비 내용은 넣지 말아 주세요.

**이름(원무과/원장님)은 확인 절차가 아닙니다.**
누가 고쳤는지 서로 알아보시라고 남기는 표시일 뿐이라, 아무나 고를 수 있습니다.
실제로 막아 주는 것은 비밀번호입니다.

**의료 표현에 주의해 주세요.**
"완치", "100% 보장" 같은 단정적인 표현은 의료광고 규정에 걸릴 수 있습니다.
"도움을 줍니다", "완화합니다" 정도로 써 주세요.

**비밀번호는 두 분만 아시게 해주세요.**
비밀번호를 바꾸고 싶으시면 4단계의 `npx wrangler secret put ADMIN_PIN` 을 다시 실행하면 됩니다.

**토큰은 90일 뒤 만료됩니다.**
만료되면 저장할 때 "저장소 접근 권한을 확인해 주세요"라는 안내가 뜹니다.
그때 1단계를 다시 해서 새 토큰을 만들고, 4단계의 `GITHUB_TOKEN` 등록만 다시 하시면 됩니다.
미리 달력에 표시해 두시면 좋습니다.

**브라우저를 닫으면 다시 비밀번호를 물어봅니다.**
공용 컴퓨터에서 쓰셨다면 화면 오른쪽 위 **잠그기** 를 눌러주세요.

---

## 잘 안 될 때

| 증상 | 확인할 것 |
|---|---|
| "저장 서버 주소가 아직 설정되지 않았습니다" | 5단계를 안 했거나, 바꾼 index.html을 push 안 한 경우 |
| "비밀번호가 맞지 않습니다" | 4단계의 `ADMIN_PIN`을 다시 등록해 보세요 |
| "허용되지 않은 주소에서의 요청입니다" | `worker/wrangler.toml` 의 `ALLOWED_ORIGINS` 주소 확인 후 `npx wrangler deploy` 다시 실행 |
| "저장소 접근 권한을 확인해 주세요" | GitHub 토큰이 만료됐거나 Contents 권한이 빠진 경우 (1·4단계 다시) |
| "다른 곳에서 먼저 저장되었습니다" | 두 분이 동시에 저장한 경우. 새로고침 후 다시 저장하시면 됩니다 |
| 공지 목록 자리에 "불러오지 못했습니다" | 잠시 후 새로고침. 계속되면 알려주세요 |
| "유튜브 영상 주소를 알아보지 못했습니다" | 유튜브 **공유** 버튼으로 나온 주소를 그대로 붙여넣어 주세요 |
| 미리보기 주소에서 "글을 찾지 못했습니다" | 주소가 잘렸거나, 그사이 그 글을 삭제한 경우입니다 |
| "최근 활동"이 비어 있음 | 이 기능을 켠 뒤에 한 변경부터 쌓입니다. 저장 직후엔 1~2분 걸립니다 |
